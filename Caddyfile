money.timur-ergashev.ru {
	encode gzip

	# ВАЖНО (без логина любой, кто знает адрес, увидит твои финансы,
	# т.к. ссылка синхронизации с Google-таблицей зашита в приложение).
	# Включи защиту паролем: сгенерируй хэш и подставь логин/хэш ниже,
	# затем раскомментируй блок:
	#
	#   docker compose run --rm caddy caddy hash-password --plaintext 'ТВОЙ_ПАРОЛЬ'
	#
	# basicauth {
	#     timur ХЭШ_ИЗ_КОМАНДЫ_ВЫШЕ
	# }

	reverse_proxy app:3000
}
