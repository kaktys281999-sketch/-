"use client";

// Кнопка «Сохранить» убрана из шапки: при включённой автосинхронизации данные
// сохраняются автоматически, а ручная отправка осталась в Настройках → Синхронизация.
export function SaveButton() {
  return null;
}
